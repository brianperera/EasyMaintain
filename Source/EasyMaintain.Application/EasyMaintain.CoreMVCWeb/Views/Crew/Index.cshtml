@model EasyMaintain.CoreWebMVC.Models.CrewViewModel

@{
    ViewBag.Title = "Crew";
}

<div class="flex-grid no-responsive-future" style="height: 100%;">
    <div class="row" style="height: 100%">
        <div class="cell size-x200" id="cell-sidebar" style="background-color: #71b1d1; height: 100%">
            <ul class="sidebar">
                <li class="active">
                    <a href="#" onclick="LoadPartialView('CrewModel')">
                        <span class="mif-widgets icon"></span>
                        <span class="title">Crew</span>
                        <span class="counter">@Html.Raw(Model.CrewList.Count)</span>
                    </a>
                </li>
            </ul>
        </div>
        <div class="cell auto-size padding20 bg-white form-page-main-container" id="partial-view-container">
            @*By default the spare parts partial view will be loaded*@
            @Html.Partial("_CrewModel", Model)
        </div>
    </div>
</div>
<script>
    function LoadPartialView(viewName)
    {
        $('#partial-view-container').load(viewName);
    }
</script>